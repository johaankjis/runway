# Base URL of the FastAPI backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# live | fixture | auto  (auto = live API, fall back to the captured fixture on network failure)
NEXT_PUBLIC_RUNWAY_DATA_MODE=auto
